import React from 'react';

import NavigationBar from '../Navigation/Navigation';

const Header = (props) => {
    return (
        <header>
           <NavigationBar />
        </header>
    )
}

export default Header;